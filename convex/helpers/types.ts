import { DatabaseWriter, DatabaseReader } from "#_generated/server";

export type DbWriter = DatabaseWriter;
export type DbReader = DatabaseReader;
export const lastorderIdx = 1;//Stores the last idx of previous components
